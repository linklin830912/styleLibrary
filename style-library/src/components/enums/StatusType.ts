enum StatusType {
  main = "main",
  background = "background",
  disabled = "disabled",
  danger = "danger",
  ghosted = "ghosted",
  special = "special",
  basic = "basic",
}

export default StatusType;
