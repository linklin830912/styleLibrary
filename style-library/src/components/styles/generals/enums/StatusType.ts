enum StatusType {
  main = "main",
  background = "background",
  disabled = "disabled",
  danger = "danger",
  ghosted = "ghost",
  special = "special",
  basic = "basic",
}

export default StatusType;
